FILE(GLOB SRC ${SRC} *.c)
FILE(GLOB HDR ${HDR} *.h)

SET(HTS_ENGINE_LIB_NAME ${MAGE_LIB_NAME}-htsengine)

ADD_LIBRARY(${HTS_ENGINE_LIB_NAME} ${SRC} ${HDR})
TARGET_LINK_LIBRARIES(${HTS_ENGINE_LIB_NAME} m)
